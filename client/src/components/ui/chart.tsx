// Chart component removed to reduce bundle size
// This component was pulling in recharts (37 packages) but was not being used anywhere
// Removed to save ~20KB in bundle size

export {}; // Empty export to satisfy TypeScript